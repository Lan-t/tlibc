#include "../src/includes/use.h"

#pragma warning(push)
#pragma warning(disable)

extern size_t putsn(char*, size_t);

extern size_t puts(const char*);

extern size_t putr(void*);

#pragma warning(pop)